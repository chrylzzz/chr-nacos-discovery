server:
  port: 56010

spring:
  application:
    name: app-provider
  cloud:
    nacos:
      # 服务发现
      discovery:
      # 生产者 服务注册的地址
        server-addr: 127.0.0.1:8848
logging:
  level:
    root: info
    org.springframework: info
